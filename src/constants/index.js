import * as types from './ActionTypes';
import defaultAdminState from './DefaultAdminState';
import defaultMoveState from './DefaultMoveState';

export default {
  types: types,
  defaultAdminState: defaultAdminState,
  defaultMoveState: defaultMoveState,
}
